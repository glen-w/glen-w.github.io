# PDF Metadata Updating Dependencies
# Install these packages to enable PDF metadata updating functionality

# Primary PDF library (recommended)
PyPDF2>=3.0.0

# Alternative PDF library (fallback)
# pikepdf>=8.0.0

# Note: Install either PyPDF2 or pikepdf, not both
# PyPDF2 is lighter and more widely compatible
# pikepdf is more powerful but has more dependencies
