{
  "devDependencies": {
    "@shopify/prettier-plugin-liquid": "1.4.0",
    "prettier": "3.1.1"
  },
  "scripts": {
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "check:failures": "python3 scripts/check_failures_readiness.py",
    "check:links": "python3 scripts/check_links.py",
    "check:links:site": "python3 scripts/check_links.py --site _site",
    "check:links:ci": "python3 scripts/check_links.py --site _site --ci-parity",
    "prerelease:format": "npm run format:check",
    "prerelease:links": "npm run check:links && npm run check:links:ci"
  }
}
