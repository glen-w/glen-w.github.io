# Copy to .env (gitignored) and fill in. Never commit real values.
# Tailscale MagicDNS / CGNAT hosts belong only here — not in source.

# Twenty CRM (Untangle) — used by processing/library/twenty_people.py --fetch
TWENTY_API_KEY=
TWENTY_BASE_URL=

# Citation / scholar pipelines (optional)
OPENALEX_API_KEY=
SEMANTIC_SCHOLAR_API_KEY=
SERPAPI_API_KEY=
